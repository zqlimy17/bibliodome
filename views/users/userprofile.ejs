<!DOCTYPE html>
<html lang="en">

<head>
    <% include ../partials/head.ejs %>
    <title>Bibliodome | Where Words Come To Life</title>
</head>

<body>
    <div class="wrapper">
        <% include ../partials/nav.ejs %>
        <main>
            <div class="container">
                <h2>Welcome, <%=currentUser.name%>!</h2>
                <div class="row">
                    <div class="col s12 m3">
                        <h4>Your Profile</h4>
                        <p>Name: <%=currentUser.name%></p>
                        <p>Username: <%= currentUser.username%></p>
                        <a href="/users/profile/<%=currentUser.username%>/edit">Edit Profile</a>
                    </div>
                    <div class="col s12 m9">
                        <h4>Your Reviews</h4>
                        <% for (let i = 0; i < reviews.length; i++) {%>
                        <div class="row">
                            <div class="col s12 m6">
                                <div class="card blue-grey darken-1">
                                    <div class="card-content white-text">
                                        <span class="card-title"><%=reviews[i].book.title%></span>
                                        <p>Your Review: <%=reviews[i].review%></p>
                                        <p>Your Rating: <%=reviews[i].rating%></p>
                                    </div>
                                    <div class="card-action">
                                        <a href="/reviews/<%=reviews[i].book.id%>/edit-review">Edit</a>
                                        <a href="/reviews/<%=reviews[i]._id%>/<%=reviews[i].book.id%>">Delete</a>
                                    </div>
                                </div>
                            </div>
                        </div>



                        <h5></h5>

                        <%}%>
            </div>
        </div>
    </div>
    </main>
    <% include ../partials/footer.ejs %>
                    </div>
</body>

</html>